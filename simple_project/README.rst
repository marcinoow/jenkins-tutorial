Celery connects to redis Redis with password
============================================

Usage:
------

1. Create services:

   ``$ docker-compose up --build``

2. Test app:

   ``$ docker-compose up --build``
